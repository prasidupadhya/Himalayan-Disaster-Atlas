# Raw acquisition

Ignored immutable source snapshots, organized by source/dataset and digest or version. Never import into the web app or place credentials here. The sample pipeline populates this directory from its committed synthetic CSV.
