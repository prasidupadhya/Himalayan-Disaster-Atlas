# No frontend environment variables are required or permitted in Foundation.
# Future acquisition credentials belong only in an ignored, pipeline-only file.
# Never use NEXT_PUBLIC_ for a credential; never put .env files in apps/web.
